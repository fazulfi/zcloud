// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Wei-Shaw/sub2api/ent/modelcatalog"
)

// ModelCatalog is the model entity for the ModelCatalog schema.
type ModelCatalog struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// CanonicalName holds the value of the "canonical_name" field.
	CanonicalName string `json:"canonical_name,omitempty"`
	// PublicName holds the value of the "public_name" field.
	PublicName string `json:"public_name,omitempty"`
	// ContextWindow holds the value of the "context_window" field.
	ContextWindow *int64 `json:"context_window,omitempty"`
	// SourceSuppliers holds the value of the "source_suppliers" field.
	SourceSuppliers []string `json:"source_suppliers,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModelCatalogQuery when eager-loading is set.
	Edges        ModelCatalogEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ModelCatalogEdges holds the relations/edges for other nodes in the graph.
type ModelCatalogEdges struct {
	// Pricing holds the value of the pricing edge.
	Pricing []*ModelPricing `json:"pricing,omitempty"`
	// SupplierPricing holds the value of the supplier_pricing edge.
	SupplierPricing []*SupplierPricing `json:"supplier_pricing,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PricingOrErr returns the Pricing value or an error if the edge
// was not loaded in eager-loading.
func (e ModelCatalogEdges) PricingOrErr() ([]*ModelPricing, error) {
	if e.loadedTypes[0] {
		return e.Pricing, nil
	}
	return nil, &NotLoadedError{edge: "pricing"}
}

// SupplierPricingOrErr returns the SupplierPricing value or an error if the edge
// was not loaded in eager-loading.
func (e ModelCatalogEdges) SupplierPricingOrErr() ([]*SupplierPricing, error) {
	if e.loadedTypes[1] {
		return e.SupplierPricing, nil
	}
	return nil, &NotLoadedError{edge: "supplier_pricing"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ModelCatalog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case modelcatalog.FieldSourceSuppliers:
			values[i] = new([]byte)
		case modelcatalog.FieldContextWindow:
			values[i] = new(sql.NullInt64)
		case modelcatalog.FieldID, modelcatalog.FieldCanonicalName, modelcatalog.FieldPublicName, modelcatalog.FieldStatus:
			values[i] = new(sql.NullString)
		case modelcatalog.FieldCreatedAt, modelcatalog.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ModelCatalog fields.
func (_m *ModelCatalog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case modelcatalog.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case modelcatalog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case modelcatalog.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case modelcatalog.FieldCanonicalName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field canonical_name", values[i])
			} else if value.Valid {
				_m.CanonicalName = value.String
			}
		case modelcatalog.FieldPublicName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field public_name", values[i])
			} else if value.Valid {
				_m.PublicName = value.String
			}
		case modelcatalog.FieldContextWindow:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field context_window", values[i])
			} else if value.Valid {
				_m.ContextWindow = new(int64)
				*_m.ContextWindow = value.Int64
			}
		case modelcatalog.FieldSourceSuppliers:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field source_suppliers", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.SourceSuppliers); err != nil {
					return fmt.Errorf("unmarshal field source_suppliers: %w", err)
				}
			}
		case modelcatalog.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ModelCatalog.
// This includes values selected through modifiers, order, etc.
func (_m *ModelCatalog) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPricing queries the "pricing" edge of the ModelCatalog entity.
func (_m *ModelCatalog) QueryPricing() *ModelPricingQuery {
	return NewModelCatalogClient(_m.config).QueryPricing(_m)
}

// QuerySupplierPricing queries the "supplier_pricing" edge of the ModelCatalog entity.
func (_m *ModelCatalog) QuerySupplierPricing() *SupplierPricingQuery {
	return NewModelCatalogClient(_m.config).QuerySupplierPricing(_m)
}

// Update returns a builder for updating this ModelCatalog.
// Note that you need to call ModelCatalog.Unwrap() before calling this method if this ModelCatalog
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ModelCatalog) Update() *ModelCatalogUpdateOne {
	return NewModelCatalogClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ModelCatalog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ModelCatalog) Unwrap() *ModelCatalog {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ModelCatalog is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ModelCatalog) String() string {
	var builder strings.Builder
	builder.WriteString("ModelCatalog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("canonical_name=")
	builder.WriteString(_m.CanonicalName)
	builder.WriteString(", ")
	builder.WriteString("public_name=")
	builder.WriteString(_m.PublicName)
	builder.WriteString(", ")
	if v := _m.ContextWindow; v != nil {
		builder.WriteString("context_window=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("source_suppliers=")
	builder.WriteString(fmt.Sprintf("%v", _m.SourceSuppliers))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// ModelCatalogs is a parsable slice of ModelCatalog.
type ModelCatalogs []*ModelCatalog
